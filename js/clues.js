var clues=["What element is atomic number 1"];
var answers=["h"];

var currentClue;
var currentAnswer;

publishClue();

function publishClue()
{
  currentClue = clues[0];
  currentAnswer = answers[0];
}
